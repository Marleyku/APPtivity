/** Activation definition for goapptivity-public. */
export const activation = {
  "grain": "visitor",
  "description": "Visitor activated when they use a primary CTA (join / learn more).",
  "events": [
    "marketing_cta_clicked"
  ]
};

export const APP_ID = 'goapptivity-public';
